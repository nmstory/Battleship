public interface IGame {
    //Grid createGrid();
    Player createPlayerOne(Grid g, String name);

    /* TO IMPLENENT:
    int turn(int previous);

    Player createPlayerTwo(Grid g, boolean AI);

    Round playerOneTurn(Grid g);
    Round playerTwoTurn(Grid g);*/
}
